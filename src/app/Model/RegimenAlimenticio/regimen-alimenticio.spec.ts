import { RegimenAlimenticio } from './regimen-alimenticio';

describe('RegimenAlimenticio', () => {
  it('should create an instance', () => {
    expect(new RegimenAlimenticio()).toBeTruthy();
  });
});
