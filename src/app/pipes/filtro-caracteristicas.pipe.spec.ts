import { FiltroCaracteristicasPipe } from './filtro-caracteristicas.pipe';

describe('FiltroCaracteristicasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCaracteristicasPipe();
    expect(pipe).toBeTruthy();
  });
});
