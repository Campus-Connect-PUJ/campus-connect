import { FiltroForosPipe } from './filtro-foros.pipe';

describe('FiltroForosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroForosPipe();
    expect(pipe).toBeTruthy();
  });
});
