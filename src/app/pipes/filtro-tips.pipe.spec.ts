import { FiltroTipsPipe } from './filtro-tips.pipe';

describe('FiltroTipsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipsPipe();
    expect(pipe).toBeTruthy();
  });
});
