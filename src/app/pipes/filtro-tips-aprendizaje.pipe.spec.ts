import { FiltroTipsAprendizajePipe } from './filtro-tips-aprendizaje.pipe';

describe('FiltroTipsAprendizajePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipsAprendizajePipe();
    expect(pipe).toBeTruthy();
  });
});
