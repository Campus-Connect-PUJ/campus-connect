import { FiltroTipsExigenciaPipe } from './filtro-tips-exigencia.pipe';

describe('FiltroTipsExigenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipsExigenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
