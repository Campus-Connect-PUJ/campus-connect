import { FiltroTematicaGurposPipe } from './filtro-tematica-gurpos.pipe';

describe('FiltroTematicaGurposPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTematicaGurposPipe();
    expect(pipe).toBeTruthy();
  });
});
